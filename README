branch = tmp_dev
version = v2.0

completed:
* importing full files at a time
* dividing files into user defined pdu chunks
* updating metadata per pdu chunk
	** pdu_id
	** last_flg
	** length
	** data
* converting between message and streaming domains
* can add headers and tails of specified lengths and values
* PDUs can survive msg->stream->msg domain conversions
* lengths of PDUs can be easily determined in message domain
* alternating byte fill, starting at 0, up to 6 bytes

todo:
* header needs length info
* TC header should be easily configurable
* division size should be easily configurable
* easy and well-defined test cases
	** 1018 byte data input
	** 1025 byte data input
	** etc
* needs to work at mod. (63,56)

